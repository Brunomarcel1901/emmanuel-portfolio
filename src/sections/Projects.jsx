import {motion} from "framer-motion";import {ArrowUpRight,ChevronDown} from "lucide-react";import {useState} from "react";import SectionLabel from "../components/SectionLabel";import {projects} from "../data/projects";export default function Projects(){const[open,setOpen]=useState(0);return <section id="projects" className="section"><SectionLabel>03 / SELECTED WORK</SectionLabel><div className="projects-head"><h2>Things I've<br/><em>been building.</em></h2><p>Product work and security experiments, presented as systems rather than a collection of screenshots.</p></div><div className="project-list">{projects.map((p,i)=>{const isOpen=open===i;return <motion.article className={`project-card ${isOpen?"open":""}`} key={p.number} layout><button type="button" className="project-trigger" onClick={()=>setOpen(isOpen?-1:i)} aria-expanded={isOpen}><span className="project-number">{p.number}</span><span className="project-main"><small>{p.category}</small><strong>{p.title}</strong></span><span className="project-status">{p.status}</span><ChevronDown className="project-chevron" size={20}/></button>{isOpen&&<motion.div className="project-detail" initial={{opacity:0,height:0}} animate={{opacity:1,height:"auto"}}><div className="project-visual"><div className="terminal-window"><span>PROJECT / {p.number}</span><div className="terminal-lines"><i/><i/><i/><i/></div><b>{p.title}</b></div></div><div className="project-description"><p>{p.summary}</p><div className="stack-row">{p.stack.map(x=><span key={x}>{x}</span>)}</div><a href="https://github.com/Brunomarcel1901" target="_blank" rel="noreferrer" className="text-link">View GitHub <ArrowUpRight size={16}/></a></div></motion.div>}</motion.article>})}</div></section>}
