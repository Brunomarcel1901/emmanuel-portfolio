import {AnimatePresence,motion} from "framer-motion";import {Check,Database,Globe2,LockKeyhole,Server} from "lucide-react";import {useState} from "react";import SectionLabel from "../components/SectionLabel";import {skillGroups} from "../data/skills";export default function Skills(){const[active,setActive]=useState(0);const current=skillGroups[active];const ActiveIcon=current.icon;return <section id="skills" className="section skills-section"><SectionLabel>02 / CAPABILITIES</SectionLabel><div className="skills-head"><h2>Built across<br/><em>multiple layers.</em></h2><p>Not just a list of technologies. These are the areas where I spend time building, experimenting and improving.</p></div><div className="cap-console"><div className="console-sidebar" role="tablist">{skillGroups.map((g,i)=><button key={g.title} type="button" role="tab" aria-selected={active===i} className={active===i?"active":""} onClick={()=>setActive(i)}><span>{g.index}</span><strong>{g.title}</strong><small>{g.kicker}</small></button>)}</div><div className="console-main"><AnimatePresence mode="wait"><motion.div key={current.title} initial={{opacity:0,x:18}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-14}} transition={{duration:.28}}><div className="console-title"><div><span>CAPABILITY / {current.index}</span><h3>{current.title}</h3></div><ActiveIcon size={30} strokeWidth={1.5}/></div><p className="console-detail">{current.detail}</p><div className="console-skills">{current.skills.map((s,i)=><div key={s}><span>{String(i+1).padStart(2,"0")}</span><strong>{s}</strong><Check size={15}/></div>)}</div></motion.div></AnimatePresence></div></div><div className="skill-footer"><span><Database size={15}/> DATA</span><span><Server size={15}/> SYSTEMS</span><span><LockKeyhole size={15}/> SECURITY</span><span><Globe2 size={15}/> WEB</span></div></section>}
