import {AnimatePresence,motion} from "framer-motion";import {Menu,X} from "lucide-react";import {useEffect,useState} from "react";
const links=[["About","about"],["Capabilities","skills"],["Projects","projects"],["Contact","contact"]];
export default function Navbar(){const[open,setOpen]=useState(false);useEffect(()=>{document.body.classList.toggle("menu-open",open);return()=>document.body.classList.remove("menu-open")},[open]);return <header className="navbar"><a className="brand" href="#top" onClick={()=>setOpen(false)}><span className="brand-mark">EC</span><span>EMMANUEL<span className="brand-dot">.</span></span></a><nav className="desktop-nav" aria-label="Primary navigation">{links.map(([l,id])=><a key={id} href={`#${id}`}>{l}</a>)}<a className="nav-contact" href="mailto:ughaoguemmanuel@gmail.com">Let's talk ↗</a></nav><button className="menu-trigger" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-label={open?"Close navigation":"Open navigation"}>{open?<X size={20}/>:<Menu size={20}/>}</button><AnimatePresence>{open&&<motion.nav className="mobile-menu" initial={{opacity:0,y:-15}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-15}} aria-label="Mobile navigation"><span className="menu-kicker">NAV / SYSTEM</span>{links.map(([l,id],i)=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}><span>0{i+1}</span>{l}<b>↗</b></a>)}<a className="mobile-mail" href="mailto:ughaoguemmanuel@gmail.com">Start a conversation</a></motion.nav>}</AnimatePresence></header>}
